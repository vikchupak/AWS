Connect via ssh client, but you connect thought `EC2_INSTANCE_CONNECT` service

Brawser(console) -> ec2 instance connect service (ssh)-> ec2

https://ip-ranges.amazonaws.com/ip-ranges.json

EC2 Security group has to allow connection from `EC2_INSTANCE_CONNECT` ip range
