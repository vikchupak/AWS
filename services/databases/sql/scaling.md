| **Service**                             | **Instance visibility in console UI** | **Scaling type**                                                                                                                    | **Writes**                                                                 | **Cluster placement** | **Auto Scaling notes**                                                                                                                                                                                                   |
| --------------------------------------- | ------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Amazon RDS**                          | Visible                               | 1 writer; only manual horizontal/vertical scaling of read replicas                                                                  | Single writer                                                              | Single region         | No Auto Scaling for read replicas; manual scaling only                                                                                                                                                                   |
| **Amazon Aurora (provisioned) cluster** | Visible                               | 1 writer; no default horizontal autoscaling of read replicas. Can be configured using **Application Auto Scaling** policy           | Single writer                                                              | Single region         | ⚠️ Auto Scaling **only improves read capacity** — Aurora adjusts the number of Aurora Replicas based on demand, but write capacity is **not scaled**. Write operations remain limited to the **single primary instance** |
| **Amazon Aurora Serverless v2**         | Visible                               | 1 writer; vertical autoscaling of read replicas                                                                                     | Single writer                                                              | Single region         | Vertical auto scaling applies to both writer and read replicas, but **writes still go to a single writer**                                                                                                               |
| **Amazon Aurora Global Database**       | Visible                               | 1 writer in primary region; horizontal autoscaling of read replicas across primary/secondary regions                                | Single writer                                                              | Multi-region          | Auto Scaling of read replicas across primary and secondary regions improves **global read capacity only**; writes remain in the **primary region**                                                                       |
| **Amazon Aurora DSQL**                  | Not visible                           | Fully serverless distributed SQL; all nodes can handle reads/writes with coordinated transactions; AWS scales compute **invisibly** | No single writer; any node can accept writes with distributed coordination | Multi-region          | Fully managed by AWS — no user-configurable Auto Scaling; compute scales **invisibly** for both reads and writes                                                                                                         |

